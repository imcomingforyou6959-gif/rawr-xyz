# Core
discord.py>=2.3.0
aiohttp>=3.8.0

# Optional – remove or comment out if you don't need them
textblob>=0.17.0          # for sentiment analysis (enable with ENABLE_SENTIMENT=true)
redis>=4.5.0              # for Redis caching (enable with ENABLE_REDIS=true)
asyncpg>=0.28.0           # for PostgreSQL storage (enable with ENABLE_POSTGRES=true)
structlog>=23.0.0         # prettier structured logs
sentry-sdk>=1.30.0        # error tracking (needs SENTRY_DSN env)
